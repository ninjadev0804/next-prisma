"use client";

import TopUpProps from "@/types/topups";
import TopUpNewRequestModal from "@/components/users/modals/topups";

const ClientTopUpTable = ({ data }: { data: TopUpProps[] }) => {
	// RENDER ANTD TABLE && DATA

	// USE MODAL
	return <></>;
};

export default ClientTopUpTable;
